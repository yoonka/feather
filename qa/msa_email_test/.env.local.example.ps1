# Example local defaults for smtp4dev
# Copy this file to .env.local.ps1 and adjust if needed

$env:SMTP_LOCAL_HOST = "localhost"
$env:SMTP_LOCAL_PORT = "25"
$env:SMTP_LOCAL_TLS  = "never"
$env:SMTP_LOCAL_AUTH = "never"
$env:SMTP_LOCAL_USERNAME = ""
$env:SMTP_LOCAL_PASSWORD = ""
